<?php
// Copy this file to production_secrets.php on the server and fill in your real credentials.
// This file should NOT be committed to version control.
// Return an associative array with your production DB credentials.

return [
    // Database host (keep 'localhost' unless instructed otherwise by your host)
    'DB_HOST' => 'localhost',

    // Database name
    'DB_NAME' => 'your_database_name',

    // Database user
    'DB_USER' => 'your_database_user',

    // Database password
    'DB_PASS' => 'your_secure_password',

    // Optional: Unix socket path (if your host uses socket instead of TCP)
    // Examples:
    // - cPanel typical: /var/lib/mysql/mysql.sock
    // - Leave null if not applicable
    'DB_SOCKET' => null,
];
